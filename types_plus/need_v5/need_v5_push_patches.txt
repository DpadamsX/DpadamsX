If (False) // Added: Jan 27, 2020. The field names and types of types_plus.need_v5 are found below.// types_plus.need_v5 is both an updatable view, and a type.//   id uuid,//   hsys_id uuid,//   facility_id uuid,//   item_id uuid,//   percent_down double precision,//   next_case_dts timestamp without time zone,//   flow integer,//   qty_circulation integer,//   qty_ready_everywhere integer,//   qty_ready_here integer,//   qty_sched_everywhere integer,//   qty_sched_here integer,//   qty_sterile_everywhere integer,//   qty_sterile_here integer,//   qty_suggest_sterilize integer,//   qty_total integer,//   ready_vs_need_defecit integer,//   sched_sort integer,//   sort_rank integer,//   suggest_transport boolean,//   next_needed citext,//   or_name citext,//   status citext,//   status_sort citext,//   when_ citext,//   qty_here integerEnd if: ($format_version_number=5) // Added: Jan 27, 2020	$clause_text:=$clause_text+PgConvert_UUID_ToPG_For_INSERT($row_o.id)+","// Calculated field: id	$clause_text:=$clause_text+PgConvert_UUID_ToPG_For_INSERT($row_o.hsys_id)+","// Calculated field: hsys_id	$clause_text:=$clause_text+PgConvert_UUID_ToPG_For_INSERT($row_o.facility_id)+","// Calculated field: facility_id	$clause_text:=$clause_text+PgConvert_UUID_ToPG_For_INSERT($row_o.item_id)+","// Calculated field: item_id	$clause_text:=$clause_text+PgConvert_Number_ToPG($row_o.percent_down)+","// Calculated field: percent_down	$clause_text:=$clause_text+PgConvert_DTS_ToPG($row_o.next_case_dts)+","// Calculated field: next_case_dts	$clause_text:=$clause_text+PgConvert_Number_ToPG($row_o.flow)+","// Calculated field: flow	$clause_text:=$clause_text+PgConvert_Number_ToPG($row_o.qty_circulation)+","// Calculated field: qty_circulation	$clause_text:=$clause_text+PgConvert_Number_ToPG($row_o.qty_ready_everywhere)+","// Calculated field: qty_ready_everywhere	$clause_text:=$clause_text+PgConvert_Number_ToPG($row_o.qty_ready_here)+","// Calculated field: qty_ready_here	$clause_text:=$clause_text+PgConvert_Number_ToPG($row_o.qty_sched_everywhere)+","// Calculated field: qty_sched_everywhere	$clause_text:=$clause_text+PgConvert_Number_ToPG($row_o.qty_sched_here)+","// Calculated field: qty_sched_here	$clause_text:=$clause_text+PgConvert_Number_ToPG($row_o.qty_sterile_everywhere)+","// Calculated field: qty_sterile_everywhere	$clause_text:=$clause_text+PgConvert_Number_ToPG($row_o.qty_sterile_here)+","// Calculated field: qty_sterile_here	$clause_text:=$clause_text+PgConvert_Number_ToPG($row_o.qty_suggest_sterilize)+","// Calculated field: qty_suggest_sterilize	$clause_text:=$clause_text+PgConvert_Number_ToPG($row_o.qty_total)+","// Calculated field: qty_total	$clause_text:=$clause_text+PgConvert_Number_ToPG($row_o.ready_vs_need_defecit)+","// Calculated field: ready_vs_need_defecit	$clause_text:=$clause_text+PgConvert_Number_ToPG($row_o.sched_sort)+","// Calculated field: sched_sort	$clause_text:=$clause_text+PgConvert_Number_ToPG($row_o.sort_rank)+","// Calculated field: sort_rank	$clause_text:=$clause_text+PgConvert_Boolean_ToPG($row_o.suggest_transport)+","// Calculated field: suggest_transport	$clause_text:=$clause_text+PgConvert_Text_ToPG($row_o.next_needed)+","// Calculated field: next_needed	$clause_text:=$clause_text+PgConvert_Text_ToPG($row_o.or_name)+","// Calculated field: or_name	$clause_text:=$clause_text+PgConvert_Text_ToPG($row_o.status)+","// Calculated field: status	$clause_text:=$clause_text+PgConvert_Text_ToPG($row_o.status_sort)+","// Calculated field: status_sort	$clause_text:=$clause_text+PgConvert_Text_ToPG($row_o.when_)+","// Calculated field: when_	$clause_text:=$clause_text+PgConvert_Number_ToPG($row_o.qty_here)// Calculated field: qty_here