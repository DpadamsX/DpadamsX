-- Excluded files.-- These are imported earlier in the build ccle before the event_triggers are defined.-- This needs to be done *early*, or else we don't capture many DDL events from the build.ddl_event_log.sql